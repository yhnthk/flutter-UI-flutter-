import 'package:flutter/material.dart';

class TutorialScaffoldWithBackButton extends StatelessWidget {
  const TutorialScaffoldWithBackButton({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Scaffold dengan Back Button")
      )
    );
  }
}